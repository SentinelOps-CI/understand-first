# DISABLED (P0-01 / P1-06): Inline commenter invoked non-existent `u analyze`
# and installed only loose pip deps instead of the understand-first package.
#
# Active thin PR path:
#   .github/workflows/understand-first-pr-analysis.yml
#   (pip install -e . ; u scan ; u diff --old/--new)
name: Understand-First Commenter Service (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Disabled workflow — see file header TODO"
        required: false
        default: "manual-only"

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled pending real CLI rewrite
        run: |
          echo "understand-first-commenter.yml is intentionally disabled."
          echo "Install the package and use u scan / u diff --old/--new if re-enabling."
          exit 0
