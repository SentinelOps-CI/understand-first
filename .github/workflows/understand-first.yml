# DISABLED (Wave 2 consolidation): Bloated PR job overclaimed side_effects / lens /
# tour / dashboard paths and mixed soft-fail theater with critical analysis.
#
# Active thin PR analysis lives in:
#   .github/workflows/understand-first-pr-analysis.yml
#   (u scan + u diff --old/--new only; package installed via pip install -e .)
name: Understand-First Analysis (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Disabled — use understand-first-pr-analysis.yml"
        required: false
        default: "manual-only"

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled in favor of thin PR analysis
        run: |
          echo "understand-first.yml is intentionally disabled."
          echo "Use understand-first-pr-analysis.yml (scan + diff --old/--new)."
          exit 0
